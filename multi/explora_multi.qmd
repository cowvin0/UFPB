---
title: Molusco Abalone
author: Gabriel de Jesus Pereira
date: '`r Sys.Date()`'
execute:
  refresh: true
  warning: false
  error: false
  eval: true
  echo: true
revealjs-plugins:
  - attribution 
  - roughnotation
  - pointer
format: 
  revealjs:
    theme: [default, style.scss]
    transition: slide
    background-transition: fade
    footer: "Gabriel de Jesus Pereira"
    scrollable: true
    preview-links: auto
editor:
  markdown:
    wrap: 72
lang: pt-b
---

```{r echo=FALSE}
set.seed(42)
library(dplyr)
library(GGally)
library(visdat)
library(viridis)
library(hrbrthemes)
library(tibble)
library(ggridges)
library(factoextra)
library(ggplot2)
library(corrplot)
library(ggfortify)
```


# Introdução

- Tipo de dado que está sendo utilizado no estudo

# Objetivo

# Leitura dos dados

## {auto-animate="true"}

```{r}
data <- readr::read_csv("abalone.csv") |>
  mutate(Sex = case_when(
                    Sex == "F" ~ "Fêmea",
                    Sex == "M" ~ "Macho",
                    TRUE ~ "Filhote")
  )

data |> 
  head() |>
  knitr::kable()
```

# Análise Exploratória

## Valores ausentes

```{r echo=FALSE}
data |> 
  vis_dat()
```

## Frequência de cada sexo

```{r echo=FALSE}
data |> 
  ggplot(aes(x = forcats::fct_infreq(Sex))) +
  geom_bar(
        aes(y = after_stat(count) / sum(after_stat(count)), fill = Sex),
        show.legend = FALSE,
  ) +
  theme_bw() +
  theme(axis.title.x = element_blank()) +
<<<<<<< HEAD
  labs(y = NULL, x = NULL) +
  scale_fill_manual(values=mako(4)[2:4])
=======
  labs(y = NULL, x = NULL) 
```

##

```{r echo=FALSE}
data |>
  ggplot(aes(x = TypeOfOrg, y = EnrollmentQty)) +
  geom_boxplot(fill = "#4c83b6", alpha = 0.2) +
  scale_fill_viridis(discrete = TRUE) +
  theme_ipsum() +
  theme(
    legend.position = "none",
    plot.title = element_text(size = 11)
  ) +
  xlab("")
```

##

```{r}
data |>
  mutate(EnrollmentQty = EnrollmentQty / 10000) |>
  ggplot(aes(color = forcats::fct_infreq(TypeOfOrg), x = EnrollmentQty)) +
  geom_density() +
  theme_bw()
>>>>>>> refs/remotes/origin/main
```

## Correlação

```{r echo=FALSE}
data <- data |>
  (\(x) {
     scaled <- x |>
       select_if(is.numeric) |>
       scale() |>
       as_tibble()

     x |>
       select_if(is.character) |>
       cbind(scaled)
  })()

data |>
  select_if(is.numeric) |>
  cor() |>
  corrplot(
      method="circle",
      col = mako(200),
      diag = FALSE,
      addgrid.col = "#00000040",
      tl.pos = "l",
      tl.cex = 0.6,
      tl.col = "black",
      tl.offset = 0.25,
      cl.length = 9,
      cl.cex = 0.6,
      cl.ratio = 0.25,
      family = "mono"
  )
```

# Componentes Principais

```{r echo=FALSE}
comp.p <- prcomp(data |> select_if(is.numeric), scale. = TRUE, center = TRUE, tol=0)
```

##

```{r}
fviz_eig(comp.p, addlabels = TRUE, barfill="#002951")
```

##

```{r}
fviz_pca_var(comp.p, col.var = "#002951",repel=TRUE)
```

##

```{r}
fviz_cos2(comp.p, choice = "var", axes = 1:2, fill="#002951")
```

##

```{r}
fviz_pca_var(comp.p, col.var = "#002951",repel=TRUE)
```

##

```{r}
fviz_pca_var(comp.p, col.var = "cos2", title = "Biplot - Componentes principais",
             gradient.cols = c("#002951", "#eeeeee","#a0000a"), repel = TRUE)
```

# Análise Fatorial

##

```{r}
af.reg <- factanal(data |> select_if(is.numeric),
                   factors = 2,
                   scores="regression", 
                   nstart = 10)
```

```{r}
auto <- autoplot(af.reg,
         data = data,
         colour = "Sex",
         label = FALSE,
         loadings = TRUE,
         loadings.colour = "#444444",
         loadings.label = TRUE,
         loadings.label.colour = "black",
         label.colour = "black",
         loadings.label.size = 5
) +
  theme_bw()
```

##

```{r echo=FALSE}
auto
```

# Considerações Finais
